//! SIMPLE FUNCTION SIGNATURE DEMO
//! Demonstrates basic function parameter passing and calling

fn identity(x: i64) -> i64 {
    x
}

fn add(a: i64, b: i64) -> i64 {
    a + b
}

fn main() -> i64 {
    add(identity(42), 42)  // Should return 42 + 42 = 84
}
